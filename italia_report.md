# CHARME du VIN イタリア 카테고리 — 시세 비교 리포트

## 수집 파이프라인

| 단계 | 건수 |
|---|---:|
| 카테고리 12 전체 게시물 | 515 |
| 병 단위 분리 | 679 |
| 2021년 이후 | 395 |
| 동일상품 중복제거(최신만) | 349 |
| 이번 실행 대상 (신뢰도 '높음' 2025~26) | 90 |

## 등급별 분포

| 등급 | 건수 | 의미 |
|---|---:|---|
| A | 42 | 매칭 검증 통과 + 할인율 30% 이상 |
| B | 35 | 생산자는 맞으나 큐베/빈티지 어긋남 |
| C | 2 | 오매칭 — 폐기 |
| D | 11 | 국내·해외 모두 데이터 없음 |

## 이 데이터셋에서 재측정한 연도별 가격 배율

동일 상품이 여러 해에 재게시된 경우를 추적해 세후 통일 후 비교했습니다.

| 간격 | n | 측정 중앙값 | 제시하신 값 | 채택 |
|---|---:|---:|---:|---|
| 1년 | 40 | 1.0 | 1.0 | 측정값 |
| 2년 | 16 | 1.044 | 1.12 | 측정값 |
| 3년 | 2 | 1.183 | 1.15 | 제시값(n 부족) |
| 6년 | 1 | 1.615 | 1.34 | 제시값(n 부족) |

1·2년 구간은 표본이 충분해 측정값을 썼습니다. 2년 구간이 **1.044로 제시하신 1.12와 크게 다릅니다.**
3년 이상은 n이 2와 1뿐이라 통계로 쓸 수 없어 제시하신 샴페인 계수를 유지했습니다.

가격이 그대로 유지된 비율은 **33.9%**로 샴페인(10.7%)보다 훨씬 높습니다. 이탈리아 와인은 재가격 빈도가 낮습니다.

> 주의: 연도별 *전체* 중앙값은 2021년 4,950엔 → 2026년 18,975엔으로 3.8배지만,
> 이는 가격 인상이 아니라 취급 상품이 고가로 바뀐 구성 변화입니다.
> 동일 상품 추적이 올바른 방법이고 그 결과가 위 표입니다.

## Wine-Searcher 접근 가능 여부

- **차단됨** — www.wine-searcher.com: HTTP 403
- 지시대로 한 번만 시도하고 우회 없이 Vivino로 전환했습니다.
- 국내가(와도씨)로 판정된 건은 **0건**입니다. 이탈리아 와인은 와도씨 색인에서 거의 잡히지 않습니다.

## 국내가 신뢰도 분포

국내가가 확보된 항목이 없어 신뢰도 분포를 낼 수 없습니다. 전량 해외가(Vivino) 기준입니다.

## 상품명 변환

- 사전 309항목 구축
- 대상 349건 중 완전변환 270건
- 이중 변환 사고 재발: `レ ディフェーゼ` → "Le **Le** Difese". 관사를 사전 항목에서 제거해 해결
- 사전 오류 수정: `トリオーネ` Trione → **Torrione**, `ガヤ エ レイ` → **Gaia & Rey**

### 미변환 토큰 (채워주실 목록)

지어내지 않고 원문으로 남긴 토큰입니다. 괄호 안은 등장 횟수.

`クロアット`(4), `ガラベル`(2), `トレッディベッリ`(2), `ボルゴアラテッラ`(2), `ジオバッファ`(2), `ソラリアイオニカ`(1), `ヴァルヴァラ`(1), `クラス`(1), `カラフリア`(1), `ピアーノ`(1), `ロセッティ`(1), `リンダ`(1), `イコーネ`(1), `ウンディチコムー二`(1), `ヴェルガ`(1), `エデュケイト`(1), `ヴィーニャマルティナ`(1), `カヴァリーニ`(1), `キニエラ`(1), `レオナルド`(1), `トォー!`(1), `アヴィア`(1), `ぺルヴィア`(1), `カンタローロ`(1), `コロニア`(1), `モンファルティーノ`(1), `サレーミ`(1), `カルーラ`(1), `アムーリ`(1), `トライーニ`(1), `アルパッソ`(1), `IGTトスカーナ`(1), `ウェンウィダンス`(1), `グリフォ`(1), `クアルタロ`(1), `サラチェーネ`(1), `ジャアルボリーナ`(1), `ブルネロディモンタルティーノリゼルヴァ`(1), `エルプンティード`(1), `アレ`(1), `ガッツェ`(1), `イルファルケット`(1), `テヌータデルファント`(1), `テッラ`(1), `ラヴォーロ`(1), `コッタ`(1), `クルニ`(1), `アベームス`(1), `マコン`(1), `ファッブリカ`(1), `ニュートン`(1), `ピエヴェサン`(1), `ラレッチャイア`(1), `ディアマ`(1), `ハイワ`(1), `ピエトラフォルテ`(1), `ピノグリージョテッレシチリアーネ`(1), `ロベルト`(1), `ハイク`(1), `ティオディエゴ`(1), `ドンゴンザロオロロソVOS`(1), `ディバルビ`(1), `デラピーヴェ`(1), `ルード`(1), `アルベア`(1), `チヴチヴ`(1), `アマービレ`(1), `ソリ`(1), `ロレンツォ`(1), `アマローネデッラ`(1), `キャンティクラシコリレゼルヴァ`(1), `ベッリ`(1), `チャボット`(1), `メンティン`(1), `ブリッコット`(1), `トスカイア`(1), `マッソアンティコ`(1), `バルバレスモンテステファノ`(1), `ピエヴェサンタレスティトゥー`(1), `カサ`(1), `フェランテ`(1), `レッジェーロ`(1), `ルンコト`(1), `ラルジ`(1), `フェッロヴィア`(1), `モントゾーリ`(1), `フォッサーティ`(1), `ラセッラ`(1), `クティッツイ`(1), `ミレア`(1), `モンフォルチーノ`(1), `ヴィニェートラ`(1), `カンポ`(1)
