# CHARME du VIN スペイン・ポルトガル 카테고리 — 시세 비교 리포트

## 수집 파이프라인

| 단계 | 건수 |
|---|---:|
| 카테고리 11 전체 게시물 | 141 |
| 병 단위 분리 | 188 |
| 2021년 이후 | 98 |
| 동일상품 중복제거(최신만) | 92 |

이 카테고리는 92건 전량을 처리했습니다(이탈리아와 달리 규모가 작아 신뢰도 구분 없이 전부).

## 등급별 분포

| 등급 | 건수 | 의미 |
|---|---:|---|
| A | 24 | 매칭 검증 통과 + 할인율 30% 이상 |
| B | 33 | 생산자는 맞으나 큐베/빈티지/품종 어긋남 |
| C | 7 | 오매칭 — 폐기 |
| D | 28 | 국내·해외 모두 데이터 없음 (미변환 토큰 포함 20건) |

## 연도별 가격 배율

이 카테고리에서도 동일 상품 재게시를 추적했지만 표본이 부족합니다.

| 간격 | n | 측정 중앙값 |
|---|---:|---:|
| 1년 | 8 | 1.0 |
| 7년 | 1 | 1.091 |
| 8년 | 1 | 1.364 |

1년 간격 n=8, 7년·8년 각 1건뿐이라 통계로 쓸 수 없습니다.
**이탈리아 카테고리에서 측정한 계수(1년 1.00 / 2년 1.044, 3년 이상은 제시하신 값)를 그대로 적용**했습니다.

## Wine-Searcher 접근 가능 여부

- **차단됨** — www.wine-searcher.com: HTTP 403
- 지시대로 한 번만 시도하고 우회 없이 Vivino로 전환했습니다.

## 국내가 신뢰도 분포

- 와도씨 국내가로 판정된 건: **0건** (이탈리아와 동일)
- 기준가 전량이 Vivino 단일 판매처 호가입니다. 평균가가 아닙니다.

## 상품명 변환

- 스페인·포르투갈 전용 사전 114항목을 별도로 구축
- 완전변환 63/92건
- 이탈리아 사전과 합치지 않은 이유: `ヴィーニャ`(Vigna↔**Viña**), `カステロ`(Castello↔**Castillo**),
  `ヴェルデーリョ`(Verdelho, 마데이라) vs `ヴェルデーニョ`(Verdejo, 루에다)가 충돌합니다.
- 이 카테고리에는 독일 와인이 섞여 있습니다(Clemens Busch, Rheingau Riesling, Grauburgunder).

### 미변환 토큰 (채워주실 목록)

`クランプ`(3), `コーラル`(2), `マー`(2), `セルヴェラ`(2), `ウカン`(2), `サティス`(2), `デイ`(2), `18メセス`(1), `ヴェレタ`(1), `ソラーノ`(1), `セラル`(1), `ヴァルダヤ`(1), `ヴィンクロ`(1), `アザ`(1), `ジェルミナール`(1), `フロールドランジュ`(1), `ザロジャー`(1), `ヴィニャアンダンサ`(1), `ショーンレバーブブルムレイン`(1), `ガントラム`(1), `レオナルド`(1), `ラフェ`(1), `センデロ`(1), `ペペルイ`(1), `カルチェロ`(1), `シエント80`(1), `エスピノ`(1), `デメディナ`(1), `エン`(1), `ヴァル`(1), `ガリル`(1)
